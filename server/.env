# Environment Variables
PORT=3001
CLIENT_URL=http://localhost:5173
NODE_ENV=development

# Database Configuration
# Supports both SQLite (local development) and PostgreSQL (production)
# Default: SQLite (DATABASE_TYPE=sqlite)
# For PostgreSQL: DATABASE_TYPE=postgresql

# Database Type: 'sqlite' or 'postgresql'
DATABASE_TYPE=sqlite

# SQLite Configuration (used when DATABASE_TYPE=sqlite)
# DB_STORAGE_PATH=./data  # Optional: custom path for SQLite file, default is server root

# PostgreSQL Configuration (used when DATABASE_TYPE=postgresql)
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=your_password
# DB_NAME=lingopad
# DB_SSL=false
# DB_POOL_MIN=0
# DB_POOL_MAX=5
# DB_POOL_ACQUIRE=30000
# DB_POOL_IDLE=10000

# API Keys
# Hugging Face API (Free - no key required for public models)
# HUGGINGFACE_API_KEY=your_huggingface_key_here (optional for private models)

# AWS Translate (optional)
# NOTE: The credentials below are EXPIRED/INVALID. You need to generate new ones.
# Error: "The security token included in the request is invalid"
# This is because AWS temporary credentials have a limited TTL (Time To Live)
# 
# To get fresh AWS credentials, you have two options:
#
# OPTION 1: Use AWS CLI (Recommended for Development)
# 1. Install AWS CLI: https://aws.amazon.com/cli/
# 2. Run: aws configure sso
# 3. Or run: aws configure (for permanent credentials)
# 4. Then run: aws sts get-caller-identity (to verify)
# 5. Check ~/.aws/credentials for your access key and secret key
# 6. Update the values below
#
# OPTION 2: Use AWS Management Console
# 1. Go to AWS Management Console (https://console.aws.amazon.com)
# 2. Search for "Security Credentials" or go to IAM > Users > Your User
# 3. Under "Access keys", create a new access key
# 4. Get AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY
# 5. Update the values below
# 6. For temporary credentials (with session token), use AWS STS (Security Token Service)
#    - Command: aws sts assume-role --role-arn arn:aws:iam::ACCOUNT:role/ROLE_NAME --role-session-name ROLE_SESSION_NAME
#
# OPTION 3: Disable AWS Translate (Use Hugging Face API instead)
# The app will automatically fallback to Hugging Face API (free, no credentials needed)
# Simply comment out or leave empty: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY
#
# Once you get new credentials, paste them below:
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=

# Email Configuration for Contact Form
# To use Gmail, you need to:
# 1. Enable 2-factor authentication on your Gmail account
# 2. Generate an "App Password" for LingoPad
# 3. Use the App Password below (not your regular password)

EMAIL_USER=shankar4112004@gmail.com
EMAIL_PASS=yxnd qsva abwr efrv

# Instructions to get Gmail App Password:
# 1. Go to your Google Account settings
# 2. Security -> 2-Step Verification (enable if not enabled)
# 3. App passwords -> Select "Mail" -> Generate password
# 4. Copy the generated 16-character password and paste it above

# Alternative email services you can use:
# For Outlook/Hotmail, change service to 'hotmail'
# For Yahoo, change service to 'yahoo'
# For custom SMTP, replace service with host, port, and secure settings